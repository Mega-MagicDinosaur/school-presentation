import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'presentation-1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
